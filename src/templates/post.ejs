<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= post.title %> - skyler</title>
    <meta name="description" content="<%= post.title %>" />
    <link rel="stylesheet" href="/assets/css/main.css" />
    <link rel="stylesheet" href="/assets/lib/katex/katex.min.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1><a href="/">skyler</a></h1>
      </header>

      <article>
        <header class="post-header">
          <h1 class="post-title"><%= post.title %></h1>
          <time class="post-date">最后更新：<%= post.dateFormatted %></time>
        </header>

        <div class="post-content"><%- post.html %></div>

        <nav class="post-nav">
          <% if (post.prev) { %>
          <a href="/posts/<%= post.prev.slug %>.html" class="prev">
            <span class="label">上一篇</span>
            <span class="title"><%= post.prev.title %></span>
          </a>
          <% } %> <% if (post.next) { %>
          <a href="/posts/<%= post.next.slug %>.html" class="next">
            <span class="label">下一篇</span>
            <span class="title"><%= post.next.title %></span>
          </a>
          <% } %>
        </nav>

        <a href="/" class="back-home">&larr; 返回首页</a>

        <section class="comments">
          <h3 class="comments-title">评论</h3>
          <script
            src="https://giscus.app/client.js"
            data-repo="forkicksinuk/personal"
            data-repo-id="R_kgDOQxs_ug"
            data-category="Announcements"
            data-category-id="DIC_kwDOQxs_us4C0ua4"
            data-mapping="pathname"
            data-strict="0"
            data-reactions-enabled="1"
            data-emit-metadata="0"
            data-input-position="bottom"
            data-theme="preferred_color_scheme"
            data-lang="zh-CN"
            crossorigin="anonymous"
            async
          ></script>
        </section>
      </article>
    </div>

    <script defer src="/assets/lib/katex/katex.min.js"></script>
    <script
      defer
      src="/assets/lib/katex/contrib/auto-render.min.js"
      onload="renderMathInElement(document.body, {delimiters: [{left: '$$', right: '$$', display: true}, {left: '$', right: '$', display: false}]});"
    ></script>
  </body>
</html>
